# ML artifacts directory
# Model files (.pkl) are generated by: python scripts/train_model.py
# ─────────────────────────────────────────────────────────
# This file exists only to commit the directory to git.
# The actual .pkl files are excluded via .gitignore.
